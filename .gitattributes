# Preserve exact delivery bytes and source hashes across Git checkouts.
* -text whitespace=cr-at-eol
# Keep CSV bytes stable for manifest checks, including Excel-compatible BOM/CRLF.
*.csv -text whitespace=cr-at-eol
*.blend binary
*.glb binary
*.png binary
